from pydantic_settings import BaseSettings
from typing import List


class Settings(BaseSettings):
    database_url: str = "postgresql+asyncpg://rootmap:rootmap@postgres:5432/rootmap"
    database_url_sync: str = "postgresql://rootmap:rootmap@postgres:5432/rootmap"

    jwt_secret_key: str = "dev-secret-key"
    jwt_algorithm: str = "HS256"
    access_token_expire_minutes: int = 1440

    bkt_service_url: str = "http://bkt:8001"
    selector_service_url: str = "http://selector:8002"
    planner_service_url: str = "http://planner:8003"

    cors_origins: List[str] = ["http://localhost:5173", "http://localhost:3000"]
    environment: str = "development"
    log_level: str = "INFO"

    class Config:
        env_file = ".env"
        extra = "ignore"


settings = Settings()
